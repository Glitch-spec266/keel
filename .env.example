# Copy to .env and fill in from your Supabase project settings (Settings → API).
# TODO(human): create a Supabase project, run supabase/migrations/*.sql then supabase/seed.sql,
# and paste the project URL + anon key here. Never commit the real .env.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
